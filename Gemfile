source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.6.1'

gem 'dotenv-rails'
gem 'pg'
gem 'puma', '~> 4.1'
gem 'rails', '~> 6.0.2'

group :development do
  gem 'listen'
end
